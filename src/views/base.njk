<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% if title %}{{ title }} - {{ siteTitle }}{% else %}{{ siteTitle }}{% endif %}</title>
        <link rel="stylesheet" href="/dist/main.css">
    </head>
    <body>
        {% block body %}{% endblock body %}
    </body>
</html>